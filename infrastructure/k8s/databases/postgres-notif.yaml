apiVersion: v1
kind: Secret
metadata:
  name: postgres-notif-secret
  namespace: lexcam
type: Opaque
stringData:
  POSTGRES_PASSWORD: lexcam_dev
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-notif-pvc
  namespace: lexcam
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: postgres-notif
  namespace: lexcam
spec:
  serviceName: postgres-notif-svc
  replicas: 1
  selector:
    matchLabels:
      app: postgres-notif
  template:
    metadata:
      labels:
        app: postgres-notif
    spec:
      containers:
        - name: postgres
          image: postgres:15
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_DB
              value: lexcam_notif
            - name: POSTGRES_USER
              value: lexcam
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: postgres-notif-secret
                  key: POSTGRES_PASSWORD
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
          resources:
            requests:
              memory: "256Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "300m"
          readinessProbe:
            exec:
              command: ["pg_isready", "-U", "lexcam", "-d", "lexcam_notif"]
            initialDelaySeconds: 10
            periodSeconds: 5
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: postgres-notif-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: postgres-notif-svc
  namespace: lexcam
spec:
  selector:
    app: postgres-notif
  ports:
    - port: 5432
      targetPort: 5432
  type: ClusterIP
