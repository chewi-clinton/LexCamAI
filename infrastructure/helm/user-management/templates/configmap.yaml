apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "user-management.name" . }}-config
  labels:
    {{- include "user-management.labels" . | nindent 4 }}
data:
  {{- range $key, $val := .Values.config }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}
