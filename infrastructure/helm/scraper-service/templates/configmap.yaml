apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "scraper-service.name" . }}-config
  labels:
    {{- include "scraper-service.labels" . | nindent 4 }}
data:
  {{- range $key, $val := .Values.config }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}
