apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "scraper-service.name" . }}
  labels:
    {{- include "scraper-service.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "scraper-service.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "scraper-service.selectorLabels" . | nindent 8 }}
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "{{ .Values.service.port }}"
        prometheus.io/path: /metrics
    spec:
      containers:
        - name: scraper-service
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.service.port }}
          envFrom:
            - configMapRef:
                name: {{ include "scraper-service.name" . }}-config
            - secretRef:
                name: {{ include "scraper-service.name" . }}-secret
          command: ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8011"]
          readinessProbe:
            httpGet:
              path: /v1/health
              port: {{ .Values.service.port }}
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /v1/health
              port: {{ .Values.service.port }}
            initialDelaySeconds: 20
            periodSeconds: 30
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
