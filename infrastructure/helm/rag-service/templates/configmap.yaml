apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "rag-service.name" . }}-config
  labels:
    {{- include "rag-service.labels" . | nindent 4 }}
data:
  {{- range $key, $val := .Values.config }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}
