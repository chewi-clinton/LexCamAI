apiVersion: v1
kind: Service
metadata:
  name: {{ include "rag-service.name" . }}-svc
  labels:
    {{- include "rag-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "rag-service.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: 8000
