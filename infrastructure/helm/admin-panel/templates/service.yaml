apiVersion: v1
kind: Service
metadata:
  name: {{ include "admin-panel.name" . }}-svc
  labels:
    {{- include "admin-panel.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "admin-panel.selectorLabels" . | nindent 4 }}
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
