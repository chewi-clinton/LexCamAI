apiVersion: v1
kind: Secret
metadata:
  name: {{ include "knowledge-base-service.name" . }}-secret
  labels:
    {{- include "knowledge-base-service.labels" . | nindent 4 }}
type: Opaque
data:
  {{- range $key, $val := .Values.secrets }}
  {{ $key }}: {{ $val | b64enc | quote }}
  {{- end }}
