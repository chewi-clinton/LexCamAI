apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "document-service.name" . }}-config
  labels:
    {{- include "document-service.labels" . | nindent 4 }}
data:
  {{- range $key, $val := .Values.config }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}
