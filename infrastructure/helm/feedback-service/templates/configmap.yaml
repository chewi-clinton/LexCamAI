apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "feedback-service.name" . }}-config
  labels:
    {{- include "feedback-service.labels" . | nindent 4 }}
data:
  {{- range $key, $val := .Values.config }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}
