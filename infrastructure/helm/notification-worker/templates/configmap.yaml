apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "notification-worker.name" . }}-config
  labels:
    {{- include "notification-worker.labels" . | nindent 4 }}
data:
  {{- range $key, $val := .Values.config }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}
