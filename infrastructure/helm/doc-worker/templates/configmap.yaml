apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "doc-worker.name" . }}-config
  labels:
    {{- include "doc-worker.labels" . | nindent 4 }}
data:
  {{- range $key, $val := .Values.config }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}
